import Route from '@ember/routing/route';

export default Route.extend({
  // TODO redirect to setup if no user/radio exists yet
  // beforeModel(){
  //   if(needsSetup) { // do some request to API
  //      this.transitionTo('setup');
  //   }
  // }
});
