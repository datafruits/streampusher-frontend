<div class="w-full">
  <TimetableCalendar
    @onTypeChange={{this.calendarTypeChange}}
    @reloadCalendar={{this.reloadCalendar}}
    @query={{this.query}}
    @startDate={{this.query.start}}
    @view={{this.view}}
  />
</div>
{{outlet}}
