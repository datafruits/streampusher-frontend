<Playlists::TracksList
  @playlist={{this.model.playlist}}
  @save={{this.savePlaylist}}
  @transitionAfterDelete={{route-action "transitionAfterDelete"}}
  @searchParams={{this.playlistsSearchParams}}
  @reorderItems={{this.reorderItems}}
  />
